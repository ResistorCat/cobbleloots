{
  "name": "cobbleloots",
  "version": "2.3.0",
  "description": "A side-mod for Cobblemon that adds different ways to obtain items like in the original games.",
  "private": true,
  "type": "module",
  "scripts": {
    "factory": "node scripts/factory-runner.mjs",
    "release": "node scripts/generate-release.mjs",
    "relay:dev": "npm --prefix infra/linear-relay run dev",
    "relay:deploy": "npm --prefix infra/linear-relay run deploy"
  }
}
